<div class="home-header">
    {{#if search}}
    <h1>Buscando por: <span>{{search}}</span></h1>
        {{#if twyttesQtd}}
        <p><span>{{twyttesQtd}}</span> resultados encontrados.</p>
        {{else}}
        <p>Nenhum resultado para <span>{{search}}</span>.</p>
        {{/if}}
    {{else}}
    <h1>Todos os twyttes</h1>
    {{/if}}
    <form action="/" method="GET">
        <input type="text" name="search" placeholder="Procurando algo?">
        <input type="submit" class="btn" value="Buscar">
    </form>
    <div class="order-container">
        <span>Ordenar por:</span>
        <form action="/" method="GET">
            <input type="hidden" name="search" value="{{this.search}}">
            <input type="hidden" name="order" value="new">
            <button type="submit"><i class="bi bi-arrow-up"></i></button>
        </form>
        <form action="/" method="GET">
            <input type="hidden" name="search" value="{{this.search}}">
            <input type="hidden" name="order" value="old">
            <button type="submit"><i class="bi bi-arrow-down"></i></button>
        </form>
        <a href="/">Limpar filtros</a>
    </div>
</div>
<div class="twyttes-container">
    {{#each twyttes}}
        <figure>
            <blockquote>
                "{{this.title}}"
            </blockquote>
            <figcaption>by <span>{{this.User.name}}</span></figcaption>
        </figure>
    {{/each}}
</div>